apply plugin: "java-library"

compileJava.options.encoding = "UTF-8"
compileTestJava.options.encoding = "UTF-8"

jar {
    archiveBaseName = "${rootAppName}-${project.name}"
}

repositories {
    maven { url "$aliyunMavenPublicUrl" }
    mavenCentral()
}